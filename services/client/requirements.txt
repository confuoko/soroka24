Django==5.1.4
# REST API поверх того же сервисного слоя, что и веб-страницы (нужен телеграм-боту).
djangorestframework==3.15.2
# Готовая регистрация/логин/токены для DRF поверх django.contrib.auth — своего кода
# аутентификации не пишем.
djoser==2.3.1
psycopg2-binary==2.9.12
# Версии брокера и клиента Redis держим теми же, что в core.
celery==5.6.3
redis==5.2.1
# Клиент ходит в core-api по HTTP.
requests==2.32.3
