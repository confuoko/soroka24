django==5.2.17
psycopg2-binary==2.9.12
# HTTP к core_v2. Единственный способ добраться до судебных данных: общей БД нет.
requests==2.32.5
# Подписка на очередь case_changes (management-команда consume_case_events, Phase 5).
# Не Celery: в очереди обычный JSON, а не celery-протокол.
pika==1.3.2
# Раздача статики без отдельного nginx.
gunicorn==23.0.0

# --- только для тестов -------------------------------------------------------
pytest==8.3.4
pytest-django==4.14.0
