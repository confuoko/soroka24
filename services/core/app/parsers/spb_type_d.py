"""Заглушка парсера страниц типа D — мировые суды Санкт-Петербурга (mirsud.spb.ru).

Разбор ЕЩЁ НЕ НАПИСАН: parse() намеренно падает с NotImplementedError.

Чем этот случай отличается от заглушки типа C (Брянск). Там регион СОЗНАТЕЛЬНО не
подключён к резолверу, чтобы дело отсекалось на входе и не тратило прокси с капчей.
Здесь наоборот: домен mirsud.spb.ru в COURT_BY_DOMAIN уже стоит, и падение на разборе
— цель, а не побочный эффект. Задача успевает сходить на портал, а снимок страницы
кладётся в S3 ДО разбора (app/monitoring/tasks.py, шаг 3b), поэтому через API можно
дёргать реальные дела и копить образцы разметки, по которым этот парсер и будет
написан. Портал бесплатный в прямом смысле: капчи на нём нет, лишних денег такие
походы не стоят.

Что уже разведано на живых карточках, чтобы не начинать с нуля:

* основные сведения лежат парами «подпись → значение», подпись — <b class="table-title">:
  № участка, Район, Судья, Сущность спора, УИД, Дата поступления,
  Дата принятия к производству, Статус;
* стороны — таблица с шапкой «ФИО / наименование» + «Вид лица»;
* движение дела — таблица с шапкой «Описание события / Результат события»,
  «Дата события», «Время события», «Дата публикации»;
* таблицы надо различать ПО СОСТАВУ ШАПКИ, а не по порядку: рядом на странице лежит
  table.personal-data — форма ввода ключа доступа к документам для участников дела,
  к публичным данным она отношения не имеет;
* УИД на странице есть в обычном общероссийском формате, так что extract_uid из
  app/courts/base.py работает как есть.

Когда разбор напишут: ключи результата должны совпасть с типами A и B (контракт в
app/parsers/base.py), иначе CaseRepository и EventRepository с ними не сработают,
а пример страницы — лечь в html_examples/ и накрыться тестами, как у остальных типов.
"""
from app.parsers.base import CaseParser


class SpbTypeDParser(CaseParser):
    """Парсер страниц типа D (mirsud.spb.ru). НЕ РЕАЛИЗОВАН."""

    page_type = "D"

    def parse(self, html: str) -> dict:
        """Пока не умеем: разбор типа D не написан.

        Падаем явно, а не возвращаем пустой словарь: пустой разбор выглядел бы как
        «дело есть, но в нём ничего нет» — карточка сохранилась бы без единого поля,
        и обход считал бы это нормой.
        """
        raise NotImplementedError(
            "Разбор страниц Санкт-Петербурга (тип D) ещё не написан — сейчас собираем "
            "образцы страниц, см. app/parsers/spb_type_d.py"
        )
