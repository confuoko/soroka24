"""Celery-таски мониторинга.

Пока пусто. Здесь появятся, например:
  - парсинг только что добавленного дела (очередь `urgent`);
  - диспетчер ежедневного обхода и обновление существующих дел (очередь `regular`).

Файл подключён в app/celery_app.py через include, поэтому воркеры стартуют
корректно даже без объявленных задач.
"""
